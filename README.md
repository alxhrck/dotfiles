## Dotfiles

### To install

```bash
install zsh
install starship
install alacritty

git clone 
cd <repo>
./install.sh

```
### Local ZSH Config 

Configurations needing to be kept separate or stored outside this repoistory should be included in `.zsh.local` which is sourced from `rc.zsh`.
